import 'package:flutter/material.dart';

const kDefaultTargetMinutes = 600000; // 10,000 hours

const kSessionMinCountedDuration = Duration(seconds: 60);

const kDefaultAccentColor = Color(0xFF14B8A6);
